.location-search-bar {
  position: relative;
  width: 100%;

  &__button {
    position: absolute;
    top: 0;
    bottom: 0;
    right: 0;
    background: var(--color-nevada);
    color: var(--color-white);
    border-radius: 4px;
    width: 100px;
  }
}